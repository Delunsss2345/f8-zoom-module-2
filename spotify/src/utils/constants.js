// URL gốc của API backend
export const API_BASE_URL = "https://spotify.f8team.dev/api";

// CSS classes dùng cho modal và UI
export const MODAL_CLASSES = {
  SHOW: "show",
  ACTIVE: "active",
};

// Keyboard event keys để xử lý phím tắt
export const KEYS = {
  ESCAPE: "Escape", // Phím ESC để đóng modal
};

// Giá trị mặc định cho pagination API
export const DEFAULT_LIMIT = 20;
export const DEFAULT_OFFSET = 0;
